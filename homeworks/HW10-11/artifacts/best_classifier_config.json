{
  "experiment_id": "C4",
  "model_summary": "ResNet18-finetune",
  "dataset": "CIFAR100",
  "img_size": 224,
  "num_classes": 100,
  "batch_size": 32,
  "epochs_trained": 2,
  "seed": 42,
  "optimizer": "AdamW",
  "lr": 0.0003,
  "criterion": "CrossEntropyLoss",
  "transforms": {
    "train": "Resize(256) -> RandomResizedCrop(224) -> RandomHorizontalFlip() -> ToTensor() -> Normalize(ImageNet)",
    "val": "Resize(256) -> CenterCrop(224) -> ToTensor() -> Normalize(ImageNet)",
    "test": "Resize(256) -> CenterCrop(224) -> ToTensor() -> Normalize(ImageNet)"
  },
  "preprocess": {
    "resize": 256,
    "crop": 224,
    "normalize_mean": [
      0.485,
      0.456,
      0.406
    ],
    "normalize_std": [
      0.229,
      0.224,
      0.225
    ]
  },
  "augmentation": [
    "RandomResizedCrop(224, scale=(0.8, 1.0))",
    "RandomHorizontalFlip()"
  ],
  "best_val_accuracy": 0.19,
  "test_accuracy": 0.1533
}